using System;

namespace CHE
{
    class Program
    {
        static void Main(string[] args)
        {
            int numero = int.MaxValue;   /* el maximo valor permitido en int */
            Console.WriteLine(numero);
            int resultado = numero+20;        /* Propiedades en el proyecto, despues entrar en compilacion
                                               * y luego en avanzadas*/
        }
    }
}
